<%- include('../partials/head') %>
<%- include('../partials/navbar') %>

<main class="container-fluid py-4">
  <div class="row mb-4">
    <div class="col">
      <h2><i class="bi bi-calendar3 me-2"></i><%= t('nav.bookings') %></h2>
    </div>
    <div class="col-auto">
      <a href="/bookings/new" class="btn btn-primary">
        <i class="bi bi-plus-circle me-1"></i><%= t('nav.newBooking') %>
      </a>
    </div>
  </div>

  <div class="card border-0 shadow-sm">
    <div class="card-body text-center py-5 text-muted">
      <i class="bi bi-calendar3 fs-1 d-block mb-2"></i>
      <p class="mb-1"><%= t('bookings.calendarComingSoon') %></p>
      <p class="small"><%= t('bookings.calendarM3Note') %></p>
    </div>
  </div>
</main>

<%- include('../partials/footer') %>
